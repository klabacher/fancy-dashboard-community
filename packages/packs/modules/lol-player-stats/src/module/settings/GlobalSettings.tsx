import type { ReactElement } from "react";

export default function GlobalSettings(): ReactElement {
  return (
    <div className="p-4 text-sm text-zinc-600 dark:text-zinc-300">
      This module has no global settings.
    </div>
  );
}
