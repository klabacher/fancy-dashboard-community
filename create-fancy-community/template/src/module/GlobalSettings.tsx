import type { GlobalSettingsProps } from "@fancydashboard/sdk/plugins/types";

export default function GlobalSettings(_props: GlobalSettingsProps) {
  return (
    <div style={{ padding: "1rem" }}>
      <p style={{ margin: 0 }}>No global settings are required for this pack.</p>
    </div>
  );
}
